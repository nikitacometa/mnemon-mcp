{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.nikitacometa/mnemon-mcp",
  "description": "Persistent layered memory for AI agents. SQLite FTS5, fact versioning, zero-cloud.",
  "repository": {
    "url": "https://github.com/nikitacometa/mnemon-mcp",
    "source": "github"
  },
  "version": "1.0.1",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "mnemon-mcp",
      "version": "1.0.1",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "MNEMON_DB_PATH",
          "description": "Path to SQLite database file (default: ~/.mnemon-mcp/memory.db)",
          "isRequired": false,
          "isSecret": false
        },
        {
          "name": "MNEMON_EMBEDDING_PROVIDER",
          "description": "Embedding provider for vector search: 'openai' or 'ollama' (default: disabled)",
          "isRequired": false,
          "isSecret": false
        },
        {
          "name": "MNEMON_EMBEDDING_API_KEY",
          "description": "API key for embedding provider (required for OpenAI)",
          "isRequired": false,
          "isSecret": true
        }
      ]
    }
  ]
}
